# Copy to .env only when you need to override Docker Compose MySQL defaults locally.
# Do not place application runtime variables here. Those belong in backend/.env.
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=ai_app_square
MYSQL_USER=ai_app_user
MYSQL_PASSWORD=ai_app_password
# The MySQL test database is created on demand by scripts/backend_test.sh.
